# Running the server with ssl

```bash
java.exe -classpath [output-path] server.Main [key-store path] [key-store password]
```

For example:

```bash
java.exe -classpath "./bin" server.Main "./herong.jks" password
```